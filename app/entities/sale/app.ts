import makeSale from './makeSale'
import makeFile from './makeFile'

const createSale = makeSale()
const creaSaleFile = makeFile()
const entity = {
  createSale,
  creaSaleFile,
}

export default entity
